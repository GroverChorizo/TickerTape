# Runtime dependencies — pinned for reproducible installs (deterministic is a
# core project value). Bump deliberately, then re-run the release gate.
httpx==0.28.1
pyarrow==23.0.0
rich==14.3.1
PyYAML==6.0.3
python-dotenv==1.2.1
pandas==3.0.0
numpy==2.4.1
ccxt==4.5.57
textual==0.79.1

# Test/dev
pytest>=7.0
